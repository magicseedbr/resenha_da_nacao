# Ingestion dependencies
feedparser>=6.0.11

# Scraping dependencies
requests>=2.31.0
beautifulsoup4>=4.12.2

# Utility dependencies
urllib3>=2.0.0

tweepy>=4.14.0

ddgs>=0.1.0

# Site builder (geração do site estático)
jinja2>=3.1.0

# Image processing (conversão webp→png para Instagram)
Pillow>=10.0.0

# LLM: geração via CLI `claude -p` (Claude Code) — ver llm.py. Sem dependência
# Python de LLM (nem google-genai nem groq); a chamada é por subprocess.