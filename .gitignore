# Segredos — nunca commitar
.env

# Sessão de login do X (cookies/auth_token) — só usada localmente; no CI vem do secret X_SESSION_JSON
x_creator/x_session.json

# Ambiente virtual Python
venv/

# Caches Python
__pycache__/
*.pyc

# macOS
.DS_Store

# Site gerado (build feito pelo GitHub Actions a partir das fontes)
site_builder/output/

# Backup local do reset (checklist de remoção manual nas redes — não versionar)
reset_backup/

# Vídeos/posts extraídos do X (binários grandes, gerados localmente)
x_video_post_extractor/extracted/

# Claude Code local settings (contém secret/paths locais — não versionar)
.claude/settings.json
